/*import { Pipe, PipeTransform } from "@angular/core";
import { RemedioComponent } from "../remedio/remedio.component";

@Pipe({
    name: "TextoReduzido"
})

export class PipeTextoReduzido implements PipeTransform {
    transform(listaRemedios: RemedioComponent[]) {
        return (listaRemedios.filter(remedio =>
          remedio.nome.length > 15 ? remedio.nome = `${remedio.nome.substr(0, 7)} ...` : remedio.nome));
    };
} */
